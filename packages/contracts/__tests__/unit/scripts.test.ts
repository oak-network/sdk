import { checkAbis } from "../../src/scripts/check-abis";
import { generateAbis } from "../../src/scripts/generate-abis";

describe("script stubs", () => {
  it("checkAbis throws TODO error", () => {
    expect(() => checkAbis()).toThrow("TODO");
  });

  it("generateAbis throws TODO error", () => {
    expect(() => generateAbis()).toThrow("TODO");
  });
});
